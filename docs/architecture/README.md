# Architecture Graphs

This folder contains architecture graphs for developers and maintainers:

- `system.mmd`: Mermaid graph for Markdown renderers.
- `system.dot`: Graphviz DOT graph for tooling that supports `.dot`.

Keep these files updated when major modules or data flow change.
